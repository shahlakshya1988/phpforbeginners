<?php
/**
 * Variable can also have boolean
 * boolean is true, or false
 * control program flow or what program should do next
 * to assign a boolean assign true or false without quotes
 * "true"!=true
 * "false"!=false
 *
 * null -> if no value has been assigned or null has been assigned
 * there are no quotes around, prefer lower case
 *
 */
$is_admin = true;
var_dump($is_admin);
$is_admin = false;
var_dump($is_admin);
$data = null;
var_dump($data);
var_dump($is_admin,$data);
