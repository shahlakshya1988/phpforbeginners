<?php
$is_admin = true;
$is_editor = false;
var_dump($is_admin);
var_dump(!$is_admin);
var_dump($is_admin && $is_editor);
var_dump($is_admin || $is_editor);
var_dump($is_admin xor $is_editor);
var_dump($is_admin xor $is_admin);
